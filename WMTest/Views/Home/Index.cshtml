@{
    ViewBag.Title = "Home Page";
}

@section featured {
    <div class="featured">
        <div class="content-wrapper">
            <hgroup class="title">
                <h1>@ViewBag.Title.</h1>
                <h2>@ViewBag.Message</h2>
            </hgroup>
			
        </div>
    </div>
    }

<h3>WebMail</h3>
<br />

@(Html.Kendo().PanelBar()
    .Name("IntroPanelBar")
    .Items(items =>
    {
        items.Add()
            .Text("Webmail")
            .Selected(true)
            .Expanded(true)
            .Content(@<text>
                <p style="padding:0 1em">Welcome to CHI Email Client...</text>);
    })
)
